version: "3.2" # because kompose conversion

services:

  score_server:
    image: docker-registry-default.10.249.20.186.nip.io:80/pushed/champion_model:2.0.4 #it's the docker registry on okd
    container_name: championmodel
    ports:
      - 8501:8501
      - 8500:8500

  score_app:
    image: docker-registry-default.10.249.20.186.nip.io:80/pushed/score_app:1.0.0
    container_name: scoreapp
    ports:
      - 9999:9999
    depends_on:
      - score_server
    volumes:
      - type: volume
        source: logs
        target: /log

  logger:
    image: docker-registry-default.10.249.20.186.nip.io:80/pushed/logger:1.0.0
    container_name: logger
    ports:
      - 9998:9998
    depends_on:
      - score_app
      - score_server
    volumes: #kompose does not support volumes_from
      - type: volume
        source: logs
        target: /log

  logdb:
    image: docker.io/postgres
    container_name: logpgsql
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: ivnard
      POSTGRES_PASSWORD: developer
      POSTGRES_DB: mydb
    restart: always

  adminer:
    image: docker.io/adminer
    restart: always
    ports:
      - 8080:8080

volumes:
  logs: